name: Update Changelog

permissions:
  contents: write
  pull-requests: read

on:
  pull_request:
    types:
      - closed
    branches:
      - main

jobs:
  update-changelog:
    name: Update Changelog
    runs-on: ubuntu-latest
    # Only run if PR was merged (not just closed)
    if: github.event.pull_request.merged == true

    steps:
      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ steps.app-token.outputs.token }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Extract PR information
        id: pr-info
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        run: |
          echo "title=$PR_TITLE" >> $GITHUB_OUTPUT
          echo "number=$PR_NUMBER" >> $GITHUB_OUTPUT
          echo "author=$PR_AUTHOR" >> $GITHUB_OUTPUT
          echo "url=$PR_URL" >> $GITHUB_OUTPUT

      - name: Parse conventional commit
        id: parse-commit
        env:
          PR_TITLE: ${{ steps.pr-info.outputs.title }}
        run: |
          # Extract type, scope, and subject from PR title
          # Format: type(scope): subject or type: subject

          if [[ "$PR_TITLE" =~ ^([a-z]+)(\(([a-z0-9-]+)\))?:\ (.+)$ ]]; then
            TYPE="${BASH_REMATCH[1]}"
            SCOPE="${BASH_REMATCH[3]}"
            SUBJECT="${BASH_REMATCH[4]}"

            echo "type=$TYPE" >> $GITHUB_OUTPUT
            echo "scope=$SCOPE" >> $GITHUB_OUTPUT
            echo "subject=$SUBJECT" >> $GITHUB_OUTPUT
            echo "valid=true" >> $GITHUB_OUTPUT
          else
            echo "valid=false" >> $GITHUB_OUTPUT
            echo "Error: PR title does not follow conventional commits format"
            exit 1
          fi

      - name: Determine changelog section
        id: changelog-section
        env:
          TYPE: ${{ steps.parse-commit.outputs.type }}
        run: |
          case "$TYPE" in
            feat)
              echo "section=### ✨ Features" >> $GITHUB_OUTPUT
              ;;
            fix)
              echo "section=### 🐛 Bug Fixes" >> $GITHUB_OUTPUT
              ;;
            perf)
              echo "section=### ⚡ Performance Improvements" >> $GITHUB_OUTPUT
              ;;
            revert)
              echo "section=### ⏪ Reverts" >> $GITHUB_OUTPUT
              ;;
            docs)
              echo "section=### 📚 Documentation" >> $GITHUB_OUTPUT
              ;;
            style)
              echo "section=### 💄 Styles" >> $GITHUB_OUTPUT
              ;;
            refactor)
              echo "section=### ♻️ Code Refactoring" >> $GITHUB_OUTPUT
              ;;
            test)
              echo "section=### ✅ Tests" >> $GITHUB_OUTPUT
              ;;
            build)
              echo "section=### 📦 Build System" >> $GITHUB_OUTPUT
              ;;
            ci)
              echo "section=### 👷 CI/CD" >> $GITHUB_OUTPUT
              ;;
            chore)
              echo "section=### 🔧 Chores" >> $GITHUB_OUTPUT
              ;;
            *)
              echo "section=### 🔄 Other Changes" >> $GITHUB_OUTPUT
              ;;
          esac

      - name: Get latest version
        id: get-version
        run: |
          # Pull latest changes (in case release workflow updated version)
          git pull origin main
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Update CHANGELOG.md
        env:
          SECTION: ${{ steps.changelog-section.outputs.section }}
          SCOPE: ${{ steps.parse-commit.outputs.scope }}
          SUBJECT: ${{ steps.parse-commit.outputs.subject }}
          PR_NUMBER: ${{ steps.pr-info.outputs.number }}
          PR_AUTHOR: ${{ steps.pr-info.outputs.author }}
          PR_URL: ${{ steps.pr-info.outputs.url }}
          VERSION: ${{ steps.get-version.outputs.version }}
        run: |
          # Create CHANGELOG.md if it doesn't exist
          if [ ! -f CHANGELOG.md ]; then
            cat > CHANGELOG.md << 'EOF'
          # Changelog

          All notable changes to this project will be documented in this file.

          The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
          and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

          ## [Unreleased]

          EOF
          fi

          # Prepare the changelog entry
          if [ -n "$SCOPE" ]; then
            ENTRY="- **$SCOPE**: $SUBJECT ([#$PR_NUMBER]($PR_URL)) by @$PR_AUTHOR"
          else
            ENTRY="- $SUBJECT ([#$PR_NUMBER]($PR_URL)) by @$PR_AUTHOR"
          fi

          # Use a temporary file for processing
          TEMP_FILE=$(mktemp)

          # Check if we need to create a new version section
          TODAY=$(date +%Y-%m-%d)

          # Read the changelog and insert the new entry
          awk -v section="$SECTION" -v entry="$ENTRY" -v version="$VERSION" -v today="$TODAY" '
          BEGIN { found_unreleased = 0; found_section = 0; inserted = 0; version_created = 0 }
          /^## \[Unreleased\]/ {
            found_unreleased = 1
            print
            next
          }
          found_unreleased && /^## \[/ && !version_created {
            # Insert version section before next version
            print ""
            print "## [" version "] - " today
            print ""
            print section
            print ""
            print entry
            print ""
            inserted = 1
            version_created = 1
          }
          found_unreleased && /^### / && !version_created {
            if ($0 == section) {
              found_section = 1
              print
              print ""
              print entry
              inserted = 1
              next
            } else if (!found_section && !inserted) {
              print section
              print ""
              print entry
              print ""
              inserted = 1
            }
          }
          { print }
          END {
            if (!inserted && !version_created) {
              print ""
              print "## [" version "] - " today
              print ""
              print section
              print ""
              print entry
            }
          }
          ' CHANGELOG.md > "$TEMP_FILE"

          mv "$TEMP_FILE" CHANGELOG.md

      - name: Commit and push changelog
        uses: actions/github-script@v7
        env:
          PR_NUMBER: ${{ steps.pr-info.outputs.number }}
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const fs = require('fs');
            const prNumber = process.env.PR_NUMBER;

            // Read updated changelog
            const changelog = fs.readFileSync('CHANGELOG.md', 'utf8');

            // Get current commit SHA
            const { data: refData } = await github.rest.git.getRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: 'heads/main'
            });
            const currentSha = refData.object.sha;

            // Get current tree
            const { data: commitData } = await github.rest.git.getCommit({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: currentSha
            });
            const currentTreeSha = commitData.tree.sha;

            // Create blob for updated changelog
            const { data: changelogBlob } = await github.rest.git.createBlob({
              owner: context.repo.owner,
              repo: context.repo.repo,
              content: Buffer.from(changelog).toString('base64'),
              encoding: 'base64'
            });

            // Create new tree
            const { data: newTree } = await github.rest.git.createTree({
              owner: context.repo.owner,
              repo: context.repo.repo,
              base_tree: currentTreeSha,
              tree: [
                {
                  path: 'CHANGELOG.md',
                  mode: '100644',
                  type: 'blob',
                  sha: changelogBlob.sha
                }
              ]
            });

            // Create commit
            const { data: newCommit } = await github.rest.git.createCommit({
              owner: context.repo.owner,
              repo: context.repo.repo,
              message: `chore(changelog): update for PR #${prNumber} [skip ci]`,
              tree: newTree.sha,
              parents: [currentSha]
            });

            // Update main branch reference
            await github.rest.git.updateRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: 'heads/main',
              sha: newCommit.sha,
              force: false
            });

            console.log(`Changelog updated for PR #${prNumber}`);
